<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/ll_title"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_alignParentTop="true"
    android:background="@color/global_ThemeColor"
    android:orientation="horizontal"
    >

    <Button
        android:id="@+id/btn_preview"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:layout_weight="1.5"
        android:background="@color/global_ThemeColor"
        android:drawableLeft="@mipmap/ic_back_white"
        android:drawableStart="@mipmap/ic_back_white"
        android:singleLine="true"
        android:textColor="@color/text_white"
        android:textSize="20sp" />

    <Button
        android:id="@+id/btn_search_vender_or_products"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginBottom="10dp"
        android:layout_marginTop="10dp"
        android:layout_weight="3"
        android:background="@mipmap/bookingonline_add"
        android:drawableLeft="@mipmap/ic_btn_search"
        android:drawableStart="@mipmap/ic_btn_search"
        android:text="@string/search_vender_and_place" />

    <Button
        android:id="@+id/btn_main_menu_settings"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:text="@string/plus"
        android:textColor="@color/text_white"
        android:background="@null"
        android:textStyle="bold"
        android:textSize="30sp"
         />

</LinearLayout>